const path = require("path");
const webpack = require("webpack");

module.exports = {
    context: process.cwd(),
    entry: {
        library: ["axios", "formik", "moment", "numeral", "react", "react-dom", "react-router-dom", "react-simple-widgets", "yup"]
    },
    output: {
        filename: "lib.dll.js",
        path: path.resolve(__dirname, "./dist"),
        library: "lib"
    },
    plugins: [new webpack.DllPlugin({ name: "lib", path: path.resolve(__dirname, "./dist/lib.json") })],
    resolve: {
        extensions: [".js", ".jsx", ".json", ".less", ".scss", ".css"],
        modules: [__dirname, "node_modules"]
    }
};
