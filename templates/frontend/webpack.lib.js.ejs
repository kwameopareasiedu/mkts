const path = require("path");
const webpack = require("webpack");

module.exports = {
    context: process.cwd(),
    entry: {
        library: ["file-saver", "formik", "react", "react-dom", "react-router-dom", "react-simple-widgets", "yup"]
    },
    output: {
        filename: "libs.dll.js",
        path: path.resolve(__dirname, "./dist/libs"),
        library: "libs"
    },
    plugins: [
        new webpack.DllPlugin({
            name: "tpg",
            path: path.resolve(__dirname, "./dist/lib/libs.json")
        })
    ],
    resolve: {
        extensions: [".js", ".jsx", ".json", ".less", ".scss", ".css"],
        modules: [__dirname, "node_modules"]
    }
};
